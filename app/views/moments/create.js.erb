$(".moments-list").append("<%= j render @moment %>");
$("#moment-<%= @moment.id %> .actions > .fa-chevron-up").click(function() {
  $(this).parent().parent().next().slideToggle('fast');
  $(this).toggleClass('fa-chevron-down fa-chevron-up');
});
$("#new_moment")[0].reset();