
<% hash = {controller: object.class.to_s.downcase.pluralize, action: 'destroy', gathering_id: @gathering.id, (object.class.to_s.downcase + '_id').to_sym => object.id } %>

<%= button_to "X", hash, method: :delete%>
<%# button_to "X", gathering_moment_path(@gathering, object), method: :delete, remote: true %>
